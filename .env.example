# ============================================================
# Энсо — переменные окружения
# Скопируйте этот файл в .env и заполните своими значениями
# ============================================================

# --- База данных PostgreSQL (локальная) -----------------------
# Формат: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://enso:CHANGE_ME_STRONG_PASSWORD@localhost:5432/enso

# --- Сервер ---------------------------------------------------
NODE_ENV=production
PORT=3000

# Включает secure-cookie для сессий (нужно при работе по HTTPS)
HTTPS=true

# Секрет для подписи сессионных cookies — придумайте длинную случайную строку
# Сгенерировать: openssl rand -hex 48
SESSION_SECRET=CHANGE_ME_TO_RANDOM_LONG_STRING
